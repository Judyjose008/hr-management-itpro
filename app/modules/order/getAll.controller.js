const baseResponse = require('../../helper/baseResponse/baseResponse');
const Order = require('./order.schema');
const {} = require('./order.validator');

const getAllOrder = async(req,res) => {
    try {
        
    } catch (error) {
        
    }
}
module.exports = getAllOrder;